# Deploys the leaderboard to Cloudflare.
#
# This runs here rather than from a laptop because it keeps the deploy credential in
# GitHub's secret store instead of on anyone's machine.
#
# One repository secret is needed, under Settings -> Secrets and variables -> Actions:
#
#   CLOUDFLARE_API_TOKEN   a token with Workers Scripts:Edit and D1:Edit
#
# The account id is in wrangler.toml. It is an identifier, not a credential - it
# appears in the address bar of the Cloudflare dashboard - so it does not need hiding.
#
# The workflow is safe to run repeatedly. It creates the database only if it is not
# already there, applies the schema with CREATE TABLE IF NOT EXISTS, and invents a
# signing secret only on the first deploy, so existing rounds are not invalidated by
# a redeploy.
name: Deploy the leaderboard

on:
  workflow_dispatch:

permissions:
  contents: read

jobs:
  deploy:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: worker
    env:
      CLOUDFLARE_API_TOKEN: ${{ secrets.CLOUDFLARE_API_TOKEN }}
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: "22"

      - name: Check the credential is there
        run: |
          if [ -z "$CLOUDFLARE_API_TOKEN" ]; then
            echo "::error::CLOUDFLARE_API_TOKEN is not set. Add it under Settings -> Secrets and variables -> Actions."
            exit 1
          fi

      - name: Install dependencies
        run: npm install --no-fund --no-audit

      - name: Test before deploying
        run: |
          npm test
          npm run test:service

      - name: Find or create the database
        id: database
        run: |
          set -euo pipefail
          id=$(npx wrangler d1 list --json | jq -r '.[] | select(.name == "quickfire-scores") | .uuid' || true)
          if [ -z "$id" ]; then
            echo "No database yet - creating one."
            npx wrangler d1 create quickfire-scores
            id=$(npx wrangler d1 list --json | jq -r '.[] | select(.name == "quickfire-scores") | .uuid')
          else
            echo "Reusing the existing database."
          fi
          if [ -z "$id" ]; then echo "::error::could not determine the database id"; exit 1; fi
          echo "id=$id" >> "$GITHUB_OUTPUT"

      - name: Point the config at it
        run: sed -i "s/PLACEHOLDER/${{ steps.database.outputs.id }}/" wrangler.toml

      - name: Apply the schema
        run: npx wrangler d1 execute quickfire-scores --remote --file=schema.sql

      - name: Set the signing secret, once
        run: |
          set -euo pipefail
          # Rotating this on every deploy would invalidate every round in progress, so
          # it is only set when the worker does not already have one.
          existing=$(npx wrangler secret list --name quickfire-scores 2>/dev/null || echo '[]')
          if echo "$existing" | grep -q SCORE_SECRET; then
            echo "Signing secret already set - leaving it alone."
          else
            openssl rand -base64 48 | npx wrangler secret put SCORE_SECRET --name quickfire-scores
          fi

      - name: Deploy
        id: deploy
        run: npx wrangler deploy 2>&1 | tee deploy.log

      - name: Prove it actually works
        run: |
          set -euo pipefail
          url=$(grep -oE 'https://[a-z0-9.-]+\.workers\.dev' deploy.log | head -1)
          echo "Testing $url"

          # A round, played through the live service exactly as the game plays one.
          # The development sandbox cannot reach workers.dev, so this is the only
          # place a deploy can be checked against the real thing.
          token=$(curl -fsS -X POST "$url/round/start" \
            -H 'content-type: application/json' \
            -d '{"pack":"mixed-bag"}' | jq -r .token)
          [ -n "$token" ] && [ "$token" != "null" ] || { echo "::error::no token"; exit 1; }

          player="smoke-$(date +%s)"
          result=$(curl -fsS -X POST "$url/round/finish" \
            -H 'content-type: application/json' \
            -d "{\"token\":\"$token\",\"player\":\"$player\",\"name\":\"Smoke test\",
                 \"right\":12,\"questions\":12,\"secondsLeft\":600,\"clues\":[]}")
          echo "$result"
          score=$(echo "$result" | jq -r .score)
          [ "$score" = "2900" ] || { echo "::error::scored $score, expected 2900"; exit 1; }

          # A made-up total must not be believed.
          token2=$(curl -fsS -X POST "$url/round/start" \
            -H 'content-type: application/json' \
            -d '{"pack":"mixed-bag"}' | jq -r .token)
          faked=$(curl -fsS -X POST "$url/round/finish" \
            -H 'content-type: application/json' \
            -d "{\"token\":\"$token2\",\"player\":\"$player-2\",\"name\":\"Faker\",
                 \"right\":12,\"questions\":12,\"secondsLeft\":600,\"clues\":[],
                 \"score\":9999999}" | jq -r .score)
          [ "$faked" = "2900" ] || { echo "::error::accepted a made-up score: $faked"; exit 1; }

          echo "Live service answered correctly."

      - name: Say where it went
        run: |
          url=$(grep -oE 'https://[a-z0-9.-]+\.workers\.dev' deploy.log | head -1 || true)
          {
            echo "### Leaderboard deployed"
            echo
            if [ -n "$url" ]; then
              echo "Address: \`$url\`"
              echo
              echo "Put this in \`data/leaderboard.json\` and push, to switch the board on."
            else
              echo "Deployed, but the address was not in the log - check the run above."
            fi
          } >> "$GITHUB_STEP_SUMMARY"
