{
  "name": "quickfire-scores",
  "version": "1.0.0",
  "private": true,
  "type": "module",
  "description": "Game server for Red Letter Day: one clock, one table a day.",
  "scripts": {
    "dev": "wrangler dev --local --port 8787",
    "test": "node --test 'test/scoring.test.js' 'test/engine.test.js'",
    "test:service": "node --test 'test/service.test.js'",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
